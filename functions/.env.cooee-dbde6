# cooee flavor — loaded automatically by firebase-tools when deploying with
# --project cooee-dbde6 (both at source-analysis time and at runtime).
# Sydney: the only data path leaving AU is the Gemini API call itself.
FN_REGION=australia-southeast1
